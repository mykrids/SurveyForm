<!-- ══════════════════════════════════════════════════════════════
     🔁 UNIVERSAL BOOTSTRAP (모든 프로젝트 공통)
     사용법: 새 프로젝트를 시작할 때 이 경계선(═══) 위쪽 블록만 복사해
     새 프로젝트의 AGENTS.md 최상단에 붙여넣으세요.
     경계선 아래쪽은 이 프로젝트 전용 설정이므로 복사하지 않습니다.
══════════════════════════════════════════════════════════════ -->

## 00. 신규 프로젝트 초기화 (세션 시작 시 1회, 이미 되어 있으면 건너뜀)

### 00.1 opencode-auto-resume 플러그인 확인/설치
- `/status`로 `opencode-auto-resume` 로드 여부를 확인한다.
- 로드되어 있지 않으면 `~/.config/opencode/opencode.jsonc`의 `plugin` 배열에
  등록한다 (전역 1회 설치이므로 이미 설치돼 있다면 등록 확인만 하면 됨).
  기본 설정값: `chunkTimeoutMs: 45000, gracePeriodMs: 3000, maxRetries: 3, subagentWaitMs: 15000, loopMaxContinues: 3`
- 등록/설치에 실패하면 조용히 넘어가지 말고 작업 시작 전에 사용자에게 알린다.
- 플러그인의 동작 방식과 에이전트 협조 규칙(task_complete 호출 시점 등)은
  이 문서 아래쪽 "0.1 LLM 멈춤 감지 플러그인" 절을 따른다.

### 00.2 옵시디언 볼트 생성/연결
- 프로젝트 루트에 `devlog/` 폴더가 없으면 생성한다.
- 프로젝트 루트에 `.obsidian/` 폴더가 없으면 빈 폴더로 생성한다.
  → 이렇게 하면 프로젝트 폴더 자체를 옵시디언 앱에서 "폴더를 볼트로 열기"로
    바로 열 수 있다. 별도 중앙 볼트로 옮기거나 심볼릭 링크할 필요 없음.
- 작업 기록은 `devlog/YYYY-MM-DD.md`에 남긴다 (아래 "0.2 토큰 5% 잔량 시
  자동 작업 기록" 절 규칙을 그대로 따름).

### 00.3 GitHub 저장소 생성
- 프로젝트 루트에 `.git`이 없으면 다음을 실행한다:
  `git init && gh repo create "$(basename "$PWD")" --private --source=. --remote=origin`
  → 저장소 이름은 자동으로 프로젝트 루트 폴더명과 동일하게 생성된다.
- 이 최초 저장소 생성 및 최초 push는 자동 실행한다.
  이후 일반 작업 중의 push도 승인 없이 자동 수행한다 (아래 6번 참고).
  단 force-push, 브랜치 삭제 등 되돌리기 어려운 git 작업은
  아래 "5. 반드시 멈추고 확인받아야 하는 경우" 규칙을 그대로 따른다.
- `gh auth status`가 실패하면(인증 안 됨) 저장소 생성을 건너뛰고
  사용자에게 알린 뒤 나머지 작업을 진행한다.

<!-- ══════════════════════════════════════════════════════════════
     ⬆️ 여기까지 복사. 아래부터는 이 프로젝트 전용 설정입니다.
══════════════════════════════════════════════════════════════ -->

# 프로젝트 자율 실행 지침 (Vibe Coding Mode)

이 문서는 opencode 에이전트가 이 프로젝트에서 작업할 때 따라야 할 기본 행동 규칙이다. 목표: 사용자가 매 단계 승인하지 않아도, 에이전트가 스스로 판단하여 코드를 작성·테스트·수정하며 작업을 끝까지 진행한다.

---

## 0. 세션 시작 시 반드시 먼저 수행할 자동 절차 (READ FIRST)

이 섹션은 **모든 세션/작업 시작 시 가장 먼저** 수행해야 하는 자동 실행 규칙이다. 아래 절차를 건너뛰고 작업을 시작하지 않는다.

### 0.1 LLM 멈춤(사망) 감지 플러그인 — 확인 및 협조

`opencode-auto-resume` 플러그인이 이미 설치되어 있다 (전역 설정 `~/.config/opencode/opencode.jsonc`의 `plugin`에 등록됨, v1.1.10).

- **플러그인 역할**: 스트림 정지, 깨진 tool call, hallucination 루프, orphan subagent parent 등을 자동 감지해 `"continue"`를 보내 재개한다. 완전 무음, UI 오염 없음.
- **에이전트가 지켜야 할 협조 규칙** (플러그인이 오판하지 않도록):
  1. 작업이 **진짜 끝났을 때만** `task_complete` 도구를 호출한다. (플러그인은 이를 완료 신호로 인식해 더 이상 continue를 보내지 않음)
  2. 긴 단일 작업(모델 학습, 대용량 빌드 등) 실행 직전에는 "지금부터 학습 시작, 예상 소요 시간 N분, 완료 시 다시 보고"라고 명시하고, 실행 중에는 30초~1분 간격으로 짧은 진행 로그를 남긴다.
  3. 같은 에러/응답이 2회 반복되면 3회째 시도 전에 접근 방식을 바꾸거나 상황을 요약해 보고한다.
  4. 승인이 필요 없는 작업인데 "계속할까요?"라고 묻고 멈추지 않는다. 판단이 서면 바로 진행한다.
- **설정값** (이미 등록됨): `chunkTimeoutMs: 45000`, `gracePeriodMs: 3000`, `maxRetries: 3`, `subagentWaitMs: 15000`, `loopMaxContinues: 3`
- **플러그인 로드 확인 방법**: opencode 내 `/status` 명령으로 `opencode-auto-resume` 표시 확인.

### 0.2 토큰 5% 잔량 시 자동 작업 기록 (AUTO-SAVE)

**전제**: 토큰 소진으로 세션이 갑자기 중단되면 그때까지의 작업 맥락이 유실될 수 있다. 이를 방지하기 위해 아래 규칙을 **항상** 따른다.

- **트리거**: 작업 도중 "토큰/컨텍스트가 약 5% 이하로 남았다"고 판단되면 (또는 컨텍스트가 매우 커져 압축 위험이 보이면) 즉시 아래 기록 절차를 수행한다.
- **수행 절차** (토큰이 소진되기 전에 반드시):
  1. `devlog/YYYY-MM-DD.md`에 현재까지의 **작업 목표 / 진행 상황 / 완료된 것 / 미완료(다음 할 일)** 를 정리·기록한다.
  2. 작업 중인 파일이 있으면 그 상태(어디까지 구현됐고 무엇이 안 됐는지)를 기록한다.
  3. 기록이 끝나면 세션을 안전하게 마무리한다. "기록 완료"를 명확히 남긴다.
- **목적**: 토큰 소진으로 갑자기 죽어도, 다음 세션이 AGENTS.md의 0.1 규칙과 devlog만 읽으면 중단 지점부터 정확히 이어갈 수 있게 한다.

### 0.3 세션 시작 점검 (오늘 어디서부터?)

매 세션 시작 시 아래를 순서대로 점검해 "오늘 작업 시작 지점"을 파악한다:

1. `devlog/`에서 가장 최근 날짜 기록 파일을 읽는다 → '다음 할 일' 섹션 확인.
2. `git status --short` + `git log -3 --oneline`으로 미커밋 변경/최근 커밋 확인.
3. 위 내용을 바탕으로 중단 지점부터 작업을 재개한다. 중단 지점이 애매하면 짧게 상황만 정리하고 넘어간다.

### 0.4 사용자 협업 규칙 (최우선 — 항상 준수)

1. **지시 → 수행**: 사용자는 지시/결정/지원만 한다. 코딩·구현·테스트·수정은 에이전트가 전문가로서 알아서 끝까지 수행한다. 사용자를 교육하려는 장황한 설명을 하지 않는다.
2. **궁금하지 않은 내용 출력 금지**: 요청하지 않은 원리 설명, 배경 교육, 장문 해설은 토큰 낭비이므로 출력하지 않는다. 결과/다음 액션 중심으로만 간결히 보고한다.
3. **에이전트가 직접 못하는 것만 요청**: 에이전트가 알아서 못하는 작업(외부 콘솔 클릭, 결제, 수동 파일 업로드, 계정 권한 부여 등)만 사용자에게 요청한다. 요청 시에는 **따라하기 순번(1. 2. 3.)** 으로 필요한 행동만 정확히 나열한다.
4. **목표 우선**: 사용자의 주업무는 코딩이 아니라 앱 완성이다. 에이전트는 사용자를 코딩 과정에 끌어들이지 않고 완성을 향해 자율 진행한다.

## 1. 기본 태도

- 지시받은 목표(Task)를 완료하는 데 필요한 하위 작업들을 스스로 계획하고, 계획을 사용자에게 나열하지 말고 **바로 실행**한다.
- 사소한 구현 방식(변수명, 파일 분리 방식, 라이브러리 내부 선택 등)은 매번 묻지 말고 합리적인 기본값으로 스스로 결정한다.
- 애매한 요구사항을 만나면, 가장 널리 쓰이는 상식적인 해석을 택해 진행하고, 무엇을 가정했는지는 마지막 요약에만 짧게 적는다. **진행을 멈추고 되묻지 않는다.**
- 한 번에 완벽한 코드를 쓰려 하지 말고, "작성 → 실행/테스트 → 실패 시 원인 분석 → 수정" 루프를 스스로 반복한다. 사용자에게 에러를 그대로 던지지 않는다.

## 2. 진행 방식

- 작업을 작은 단위(파일 단위 또는 기능 단위)로 쪼개고, 각 단위마다: 구현 → 실행(빌드/테스트/린트) → 실패 시 즉시 자체 수정, 순서로 진행한다.
- 실행 가능한 것은 반드시 직접 실행해서 확인한다. "이렇게 하면 될 것 같습니다" 식으로 추측만 하고 넘어가지 않는다.
- 여러 파일에 걸친 작업이라도 중간에 멈추지 않고, 목표가 끝날 때까지(또는 아래 5번 '중단 조건'에 해당할 때까지) 연속으로 진행한다.
- 진행 중 상태는 간결한 진행 로그로만 남긴다 (예: "✅ API 라우터 작성 → 테스트 통과", "⚠️ 에러 재현, 원인 파악 후 재시도"). 장황한 설명 없이 결과 중심으로 보고한다.

## 3. 품질 기준 (스스로 지킬 것)

- 코드 작성 후 반드시 다음을 스스로 수행한다:
  - 문법 오류 및 타입 오류 확인 (가능하면 lint/typecheck 실행)
  - 핵심 로직에 대한 최소한의 실행 테스트 (직접 실행하거나 간단한 테스트 스크립트 작성)
  - 명백한 예외 상황(빈 입력, 파일 없음, 네트워크 실패 등) 처리 여부 점검
- 기존 코드 스타일/폴더 구조가 있으면 그것을 따른다. 새로 정할 때는 이 프로젝트에서 이미 쓰인 컨벤션을 우선한다.
- 불필요하게 크게 리팩터링하지 않는다. 요청받은 범위 밖의 코드는 명백한 버그가 아니면 건드리지 않는다.

## 4. 자동 승인 범위 (물어보지 않고 진행)

아래 작업은 사용자 확인 없이 자유롭게 수행한다:

- 프로젝트 내 파일 생성/수정/이동
- 패키지 설치 (requirements.txt, package.json 등 명시된 범위 내)
- 로컬 빌드, 테스트, 린트 실행
- 로컬 개발 서버 실행/재시작
- git 커밋 및 push

## 5. 반드시 멈추고 사용자에게 확인받아야 하는 경우

아래에 해당하면 **작업을 중단하고 명확히 이유를 설명한 뒤 사용자 지시를 기다린다**:

- force-push, 브랜치 삭제 등 되돌리기 어려운 git 작업 (일반 push는 자동 수행, 아래 6번 참고)
- 프로덕션 환경 배포, 외부 서비스 API 키/비밀정보 관련 변경
- 데이터베이스 스키마 변경, 기존 사용자 데이터 삭제/마이그레이션
- 과금이 발생할 수 있는 외부 API 호출/서비스 사용
- 요구사항 자체가 모순되거나, 선택에 따라 아키텍처가 크게 갈리는 결정
- 동일한 에러를 3회 이상 자체 수정 시도했는데도 해결되지 않는 경우 — 무한 루프 대신 상황을 요약해서 보고

## 6. 커밋 규칙

- 의미 있는 작업 단위가 끝날 때마다 커밋한다 (예: "feat: 로그인 API 구현", "fix: null 참조 예외처리 추가").
- 커밋 메시지는 한글 또는 영어 conventional commit 스타일(`feat:`, `fix:`, `refactor:`, `chore:`)로 작성한다.
- 커밋 후에는 승인 없이 바로 push한다. force-push, 브랜치 삭제 등은 5번 규칙에 따라 사용자 확인을 받는다.

## 7. 응답 정지(사망) 감지 및 자동 재개

> 전제: 이 프로젝트는 `opencode-auto-resume` 플러그인이 설치되어 있다는 가정 하에 작성됨. 플러그인 동작 방식, 설정값, 에이전트 협조 규칙은 **0.1절**을 따른다. 아래는 요약이다.

- **작업이 진짜로 끝났을 때만** `task_complete` 도구를 호출한다. 텍스트만 남기고 끝내지 않는다.
- 긴 작업 도중에는 30초~1분 간격으로 진행 로그를 남기고, 실행 전에 예상 소요 시간을 명시한다.
- 동일 에러 2회 반복 시 3회째 전에 접근 방식을 바꾸거나 상황을 요약해 보고하고 멈춘다.
- 승인 불필요 작업에서 "계속할까요?"로 멈추지 않는다.

## 8. 작업 완료 시 보고

전체 작업이 끝나면 다음을 짧게 요약한다:

- 무엇을 구현/수정했는지 (파일 목록 수준)
- 스스로 내린 판단/가정 사항
- 테스트/실행 결과
- 남은 이슈나 후속 작업 제안 (있다면)

장황한 설명은 생략하고, 결과와 다음 액션 중심으로 보고한다.
