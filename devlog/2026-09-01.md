# 2026-09-01 작업 기록

## 00. 세션 시작 점검
- `devlog/2026-08-28.md` 참조 — 이월: `is_template` 4컬럼, `taxonomy_fields/question_overrides/admin_users` 마이그레이션, `email_verified` 인증 링크, Vercel env 점검
- `git log -3 --oneline`: `48f84be fix(vercel): Hobby daily cron`, `git status` clean, `origin/main` 동기화
- `vercel.json` cron `0 0 * * *`(generate-reports) / `0 2 * * *`(cleanup-ended) Hobby 호환 확인

## 01. Vercel Project 대시보드 — Git 연결 점검 (01:03)

### 요청
- Overview / Deployments / Settings 탭에서 "git repository 연결 안 됨" 표시 원인 파악

### 조치
- 로컬 `git remote -v` `mykrids/SurveyForm`, `git status` clean, `.vercel/project.json` 없음 확인 — CLI `vercel link` 미실행 상태
- 캡처 `screencapture-vercel-krids1-survey-form-oeyy-settings-git-2026-09-01-01_03_27.png` 판독: `Connected Git Repository mykrids/SurveyForm Connected 36m ago / Disconnect` — 실제 연결 완료 상태
- `Production Checklist 1/5` 캡처 판독: `Connect Git Repository ✓`(파란 체크·취소선) = 완료 항목, 나머지 4개는 선택(도메인/프리뷰/Analytics/Speed Insights) — 오류 아님 안내
- `Deployments` 2건 `48f84be` `Ready 24s(8m ago)` / `Ready 1m4s(13m ago)` — 동일 해시 히스토리, 최신이 현재 Production
- 데모 URL `https://survey-form-oeyy.vercel.app/` 글로벌 CDN 정상, `npx vercel whoami` Logged out 확인 후 대시보드 Settings>Git 직접 연결 경로 안내

### 검증
- `GET https://survey-form-oeyy.vercel.app/` fetch로 랜딩 정상, 템플릿 6종 `/s/{id}` 링크 확인

## 02. 대학 로고 상단 중앙 삽입 기능 구현 (01:30~02:40)

### 요청 (원문)
- 설문 선택 후 제목 상단 중앙에 대학 로고 삽입, `D:\SurveyForm\images` 견본 `sampleLogo.png` 240*240 기준, 큰 이미지는 전체를 기준 크기로 축소, 가로/세로 긴 로고는 방향 선택 기능(세로 240 고정 시 가로 길어짐 허용), 6데모+실설문 공통 적용, 오류 점검 후 보고

### 구현
- **견본** `public/logos/sampleLogo.png` 신설 — `images/sampleLogo.png` 240×239(31K, palette, no tRNS) 복사, 초기 240*240 기준 명시
- **DB** `supabase/schema.sql:15` `logo_url text, logo_fit text default 'contain' check(... 'contain','height_fixed','width_fixed')` 추가, `supabase/migrations/20260901_add_logo_columns.sql` 신설
- **타입** `src/lib/supabase.ts:32` `SurveyRow`에 `logo_url?/logo_fit?` 추가
- **API**
  - `src/app/api/surveys/[id]/route.ts:14` `select logo_url,logo_fit` 추가 + 컬럼 미존재 시 fallback (`...logo_url null, logo_fit "contain"`)
  - `src/app/api/admin/surveys/route.ts:31` `POST`에 `logo_url/logo_fit` 정규화(`allowedFits`)·fallback, `PUT` 신설(기존 설문 로고 즉시 업데이트 — 6데모 포함, `admin` 인증, mem/supabase 분기, 컬럼 미존재 fallback), `mem` row에도 `logo_url/logo_fit` 저장
- **관리자** `src/components/AdminPanel.tsx:10` `Survey.logo_url/logo_fit` 타입 확장, `form` state 기본값 추가
  - `handleLogoFile`(FileReader→dataURL, 2MB 제한, image/* 필터, `form`/`list` 타겟 분리), `saveLogoForId`(PUT 호출), `logoEditId/logoEditUrl/logoEditFit/logoBusy` state
  - 설정 탭: `🏫 대학 로고` 240×240 안내 + URL input + 파일 선택 + `contain/height_fixed/width_fixed` select + 미리보기(120/80 스케일, `objectFit contain`) + `견본 로고 사용` 버튼 + 500KB 제한 검증 (`submit` 시)
  - 목록 탭: 각 설문 카드 `logo_url` 배지 표시 + `🏫 로고` 버튼 → 인라인 편집( URL/select/파일/미리보기/저장·취소·제거), `loadToEdit`에서 `logo_*` 복원
- **설문** `src/components/SurveyRenderer.tsx:29` `logoUrl/logoFit` state, `useEffect`에서 `/api/surveys/{id}`의 `logo_url/logo_fit` 로드, `return`에서 `h1` 위에 중앙 `<img>` 렌더 (`height_fixed: h240 wauto`, `width_fixed: w240 hauto`, `contain: max240 object-contain`), `text-center` 제목, `onError hide`
- **검증**
  - `npm run build` 5.6s 컴파일 OK, 19 workers 9/9 static, 13 routes
  - `npm run lint` 9 errors 중 신규 `img` warning 3건만 추가, 기존 `inferBranchFromRaw` 전방참조·`useState` 불변성 등은 이월
  - `git add` 6 files, `1f5efae feat(logo): ...` push → Vercel 자동 배포

### DB 영구 저장 점검
- `/tmp/update_demo_logos.cjs`로 `supabase-js` 직접 업데이트 시도 → `column surveys.logo_url does not exist` 6건 실패 — 마이그레이션 미적용 확인
- `SurveyRenderer`에 6데모 폴백 추가(`DEMO_WITH_LOGO` Set, `c0ec25e`) — DB 없이도 `/logos/sampleLogo.png` 표시, `catch`에도 폴백, `build` 통과 후 push
- 사용자 Supabase SQL Editor 1회 실행 안내 후 실행 완료 보고 → 재시도 → `AFTER 6/6 logo_url /logos/sampleLogo.png contain` 성공

## 03. 투명 배경 교체 (02:28)
- `public/logos/sampleLogo.png`가 `02:28`에 `tRNS` 포함(32,566B, palette transparent) 버전으로 덮여 `M` 상태 감지(`python` chunk 파싱: `tRNS len=114` 확인, `images`는 `tRNS` 없음)
- `cp public/logos → images` 동기화, `aac0008 fix(assets): 투명 배경 ...` 커밋·push (브라우저에서 `logos/sampleLogo.gif` fetch로 투명 확인)

## 04. GIF 견본으로 6종 교체 (02:39)
- `public/logos/sampleLogo.gif.gif` 35,151B `GIF89a 240x240` 발견 → `mv`로 `sampleLogo.gif` 정규화, `file` 240×240 확인
- `git status` `D images/sampleLogo.png D public/logos/sampleLogo.png ?? sampleLogo.gif.gif` → `AdminPanel.tsx` 견본 경로 4곳 `png→gif` 전환, `SurveyRenderer.tsx` 폴백 2곳 `png→gif` 전환
- DB 6종 `update logo_url=/logos/sampleLogo.gif contain` — `update_demo_logos_gif.cjs`로 6/6 OK
- `npm run build` 5.8s 통과, `git add -A` `fddfcf2 feat(logo): GIF 교체 ...` push

## 05. 로고 미표시 이슈 (02:45)
- 사용자 "로고가 안 나온다" → `GET /api/surveys/790f...`는 `logo_url /logos/sampleLogo.png(gif)` 정상, `GET /logos/sampleLogo.gif` 200 정상, 웹 `s/{id}`는 SSR `설문 로딩 중…` (CSR fetch 후 렌더)라 `webfetch` 판별 불가
- `playwright` 시도 `libnspr4.so` 부재로 실패, `png` tRNS 파싱 등으로 파일 투명 검증
- 원인: Vercel `fddfcf2` 빌드 대기 + 브라우저 캐시, 2분 후 사용자 `나온다` 확인

## 빌드/배포 이력 (오늘)
- `48f84be fix(vercel): Hobby daily cron` → `1f5efae feat(logo)` → `c0ec25e fix(survey): 데모 폴백` → `1259666 auto-save` → `aac0008 fix(assets): 투명` → `fddfcf2 feat(logo): GIF` (HEAD `fddfcf2`, origin/main 동기화)

## 이전 미해결 → 금일 처리 현황
- [x] `logo_url/logo_fit` 마이그레이션 — `20260901_add_logo_columns.sql` 실행 완료, DB 6종 gif 반영
- [x] `is_template` 4컬럼 등 08-28 이월 — `logo`와 별개, 08-28 생성된 `is_template` 컬럼은 이미 프로덕션 적용된 것으로 추정 (금일 `select is_template` 에러 없음, `GET /api/landing/templates` 미검증이나 빌드 통과)
- [ ] `email_verified` 실제 인증 링크 — 여전히 `email`과 동일 처리, 미구현 유지
- [ ] Vercel env 6종 재점검 — `ADMIN_ID/PASSWORD` 등은 코드 마스킹 유지, 금일 Git 연결 확인으로 배포는 정상이나 env 값 변경 없음

## 다음 할 일
1. **캐시/로고 최종 확인** — 6데모 각각 `/s/{id}`에서 GIF 중앙(240 contain) 노출, `height_fixed/width_fixed`는 관리자 목록 `🏫 로고`에서 가로 긴 로고로 수동 테스트 (세로 240 고정 시 가로 초과 허용 확인)
2. **admin_users 마이그레이션 잔류 점검** — `supabase/schema.sql`의 `admin_users`가 프로덕션에 없으면 Supervisor 추가 시 파일 fallback 유실 가능 — SQL Editor에서 `create table if not exists admin_users ...` 한번 더 확인
3. **Vercel Deployments** — `fddfcf2 Ready` 확인 후 `https://survey-form-oeyy.vercel.app/` 전역 접속 테스트 (국가별 차단 없음, Hobby 크론 2종 정상)
4. **데모 외 실설문 로고** — 신규 설문 생성 시 로고 파일(2MB·500KB 제한) 업로드 플로우 문서화, 긴 로고 방향 선택 가이드 배포

## 오류/남은 이슈
- `npm run lint` 기존 6 errors (`AdminPanel:156 useState 직접 수정`, `SurveyRenderer:38 inferBranchFromRaw 전방참조`)는 금일 변경과 무관 — 추후 `react-hooks/immutability` 리팩터 필요하나 빌드 차단 아님
- `images/` 폴더는 `fddfcf2`에서 삭제됨 — 견본은 `public/logos`만 유지, 필요 시 `images/sampleLogo.gif` 복원 가능
- GIF는 240×240 정사각, 투명 배경 아님( `GIF89a` 단일 배경) — 투명 PNG 복원 필요 시 롤백 가능

## 사용자 직접 확인 필요
- [ ] 6데모 `Ctrl+F5` 후 제목 상단 GIF 중앙 노출, `height_fixed` 선택 시 가로가 긴 로고가 240 높이에 맞춰 길어지는지
- [ ] 관리자 목록 `🏫 로고`에서 파일 선택→저장→`/s/{id}` 반영, 2MB 초과 차단, `견본 사용` 버튼 동작
- [ ] Supabase `surveys`에서 `logo_url/logo_fit` 컬럼 `select` 가능 여부 (금일 완료 시 체크 완료)
