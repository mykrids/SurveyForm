# 2026-08-27 작업 기록

## 요청
1. 등록 목록에서 종료된 설문 선택→체크표시(✓)→삭제 (그대로)
2. 90일 자동삭제 → **30일**로 수정
3. 설문 편집 > ‘문항 불러오기’ 롤오버 컬러를 ①②③ 탭과 동일 amber로 수정

## 구현

### 1) 선택 삭제 — `src/components/AdminPanel.tsx:69,72,222,530,544`
- `selectedIds: Set<string>` 상태 + `toggleSelect`/`deleteSelected` 추가
- 목록 탭 헤더에 `선택 삭제 (n)` 버튼 + 선택 수 amber 배지, 선택 시 안내 문구
- 카드마다 체크박스(종료 설문만 활성화, 미종료 disabled), 선택 시 `border-amber`/`bg-amber-50` + 제목 앞 `✓`
- 필터/검색 변경 시 선택 초기화, 삭제는 `confirm` + `Administrator` 전용 체크
- `DELETE /api/admin/surveys` 호출, 성공 시 `load()` 갱신

### 2) DELETE API — `src/app/api/admin/surveys/route.ts:98`
- `DELETE` 핸들러: `requireAdmin` + `role===administrator` 검증, `ids: string[]` 1~20개
- Supabase 시 `found` 조회 후 `end_at < now`인 것만 `endedIds`로 필터, 미종료는 `skipped`
- `supabase.from("surveys").delete().in("id", endedIds)` (cascade로 `survey_responses_log` 자동 삭제)
- mem fallback도 유지

### 3) 30일 자동 삭제 cron — `src/app/api/cron/cleanup-ended/route.ts:1` + `vercel.json:4`
- `GET /api/cron/cleanup-ended` : CRON_SECRET / rateLimit 보호, `RETENTION_DAYS=30`
- Supabase: `end_at < cutoff(30일 전)` 조회 후 `delete().in("id", ids)`, 결과 `deleted`/`details` 반환
- mem fallback: 동일 30일 경과 필터 후 메모리 삭제
- `vercel.json` cron `0 2 * * *` 추가 (기존 generate-reports `0 * * * *` 유지)

### 4) 문항 불러오기 hover — `src/components/AdminPanel.tsx:424`
- `hover:bg-zinc-800` → `hover:bg-amber-600 dark:hover:bg-amber-500 hover:text-white` + `transition`
- 탭 비활성 hover (`hover:text-amber-700 hover:bg-amber-50`)와 동일 계열로 통일

## 검증
- `npm run build` 11.2s + TS 18.3s 통과, 13 routes (`/api/cron/cleanup-ended` 포함, `/api/admin/surveys` DELETE 동반)
- `git push` `24a5372` (2692f9d..24a5372)

## 남은 것
- 브라우저 수동 확인: `/admin` 목록 탭 → 종료 필터 → 체크박스/✓/삭제, 문항 불러오기 hover amber
- Supabase 프로덕션 마이그레이션 (`taxonomy_fields`/`question_overrides`/`admin_users`) — 08-26 이월
- Vercel env 6종 점검 후 재배포, cron Secret 동작 확인
