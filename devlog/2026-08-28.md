# 2026-08-28 작업 기록

## 요청
- 실 고객 설문 중 우수 사례를 데모 템플릿으로 승격 → 랜딩 6종 아래 자동 추가, + 버튼 아코디언으로 펼침/접기, 총 15개(6+9) 제한

## 구현
- **DB** `supabase/schema.sql:15` `is_template boolean, template_category/text, template_color/text, template_order int` 추가, `src/lib/supabase.ts:17` `SurveyRow` 확장
- **API** `src/app/api/admin/surveys/route.ts:98` `PATCH` — Administrator 전용, 고정6종 보호, 동적 9개 초과 400 차단, mem/prod 분기. `src/app/api/landing/templates/route.ts:1` 신설 — 고정6 제외 9개, 마이그레이션 전 probe로 빈 배열 반환(500 방지), mem fallback 정렬
- **AdminPanel** `src/components/AdminPanel.tsx:10,42,82,231,600` — `DEMO=isFixed||is_template` 통합, `canPromote` 실설문만, ★ 등록/해제 버튼·모달(카테고리 CSAT/Education/Event, 색상 6종), `templateModal` 상태, `confirmTemplateRegister` PATCH 호출, 데모 필터 통합
- **Landing** `src/components/Landing.tsx:2,16,75` — `useEffect fetch /api/landing/templates` → `dynamicTemplates` 9개, 기본 6종 grid 유지 + `+ 추가 템플릿 보기 (n/9)` 아코디언 버튼(접힘 기본, 펼침 시 violet 카드 grid), 총15종 표시
- **보호** `DELETE`/`cleanup-ended`에서 `is_template` 제외, 템플릿 삭제 불가, 30일 자동삭제 제외

## 검증
- `npm run build` 10.2s/16.7s 통과, 14 routes (`/api/landing/templates` 포함)
- dev `http://localhost:3000/api/landing/templates` 200 `{"templates":[],"warning":"is_template 마이그레이션 전 ..."}` — 서버 크래시 없이 빈 배열로 동작, 마이그레이션 후 동적 노출
- `http://localhost:3000/` / `http://localhost:3000/admin` 200 유지, `fuser -k 3000` + `.next` 재생성으로 안정화

## 다음 할 일 (마이그레이션 후)
- Supabase SQL Editor에서 `supabase/schema.sql` 신규 컬럼 적용: `alter table surveys add column if not exists is_template boolean ...` 등 4개
- /admin 목록에서 실설문 ★ 등록 → 랜딩 + 버튼 펼치면 노출 확인 (9개 제한, 10번째 차단)
- Vercel 재배포 후 랜딩 확인

---

## 2026-08-28 종합 — 오늘 한 일 전체

### 1) 등록 목록 3버튼 롤오버 가독성 (`fc0e0e8`)
- `src/components/AdminPanel.tsx:610` `편집 불러오기/응답 페이지` hover를 `hover:bg-zinc-50` → `hover:bg-zinc-900 hover:text-white dark:hover:bg-white dark:hover:text-zinc-900` 반전으로 수정 — 흰바탕 검정글자 대비 확보, `문항 불러오기` amber와 별개로 가장 간단한 고대비 방식 채택

### 2) JSON 버튼 정리 (`fc0e0e8`)
- 원인: `src/app/api/forms/[id]:66` `NextResponse.json`이 minified JSON을 반환 → 브라우저가 한 줄 TXT로 폭 가득 표시. 원래 krids 서비스계정 파싱 디버깅용이었으나 일반 운영 불필요
- 조치: 목록에서 `JSON` 버튼 제거 — 디버그는 직접 `GET /api/forms/{formId}` 호출로 대체

### 3) 데모템플릿 구분 (`fc0e0e8`)
- `DEMO_SURVEY_IDS` 6종을 `is_template`과 통합: `isDemo = FIXED || is_template`, 필터 `demo`에 통합, 보라 배지(`데모템플릿`/`추가 템플릿`), 편집·체크박스·삭제 불가, `DELETE`/`cleanup-ended` 모두 보호

### 4) 실설문 → 데모템플릿 승격 + 랜딩 아코디언 (`9101fd6`)
- **수요**: 실 고객 우수 설문을 데모로 승격해 소비자 유입 → 주문 유도, 기본 6종 아래 자동 추가, 지속적으로 15종(6+9)까지 확장
- **DB/타입**: `is_template/template_category/color/order` 4컬럼
- **API**: `PATCH /api/admin/surveys` (Administrator 전용, 고정6 보호, 9개 초과 400), `GET /api/landing/templates` (고정6 제외 9개, 마이그레이션 전 빈 배열로 500 방지)
- **관리자**: 목록 카드 `★ 템플릿 등록/해제` 버튼·모달(카테고리/색상), `confirmTemplateRegister`로 PATCH, 9개 초과 시 차단
- **랜딩**: `Landing.tsx:16` `fetch /api/landing/templates` → `dynamicTemplates`, 기본 6종 유지 + `+ 추가 템플릿 보기 (n/9) 총 6+n종` 아코디언(접힘 기본, 펼침 시 violet 카드 grid)
- **검증**: `npm run build` 14 routes, `GET /api/landing/templates` 200 `{"templates":[],"warning":"is_template 마이그레이션 전 ..."}` — 마이그레이션 전에도 500 없이 동작, `fuser -k 3000` + `.next` 재생성으로 안정화, `http://localhost:3000/` / `http://localhost:3000/admin` 200 유지

---

## 미해결 / 대기 (다음 세션에서 처리)

1. **Supabase 프로덕션 마이그레이션 (필수, SQL Editor)**
   - `taxonomy_fields` / `question_overrides` / `admin_users` (08-26 이월) + `is_template` 4컬럼 (`supabase/schema.sql:15`)
   - 실행: `alter table surveys add column if not exists is_template boolean not null default false; alter table surveys add column if not exists template_category text; alter table surveys add column if not exists template_color text; alter table surveys add column if not exists template_order int;`
2. **Vercel env 6종 점검 및 재배포** — `ADMIN_ID/PASSWORD`, `SUPERVISOR_*`, `AUTH_SECRET`, `CRON_SECRET`, `GAS_SHARED_SECRET`, `Resend` 등, `vercel.json` cron 2종(`generate-reports` 0 * * * *, `cleanup-ended` 0 2 * * *) 동작 확인
3. **`email_verified` 인증 링크 플로우** — 현재 `email`과 동일 처리, 실제 메일 인증 구현 대기
4. **GAS WebApp 재배포** — 시트마다 `gas/Code.gs` 배포 URL 갱신 필요 시 `CURRENT_VALUES.gasUrl` 갱신

---

## 사용자 직접 테스트 필요 (결과 회신 요망)

> 아래는 에이전트가 자동 검증할 수 없어 사용자가 브라우저에서 직접 확인해야 하는 항목입니다. `http://localhost:3000` 기준으로 체크 후 결과를 알려주세요.

### A. 종료 설문 선택 삭제 (30일 자동삭제 포함)
- [ ] `http://localhost:3000/admin` 로그인(Administrator) → 목록 탭 → `종료됨` 필터 → 종료된 설문 체크박스 활성화 확인, 미종료는 disabled
- [ ] 체크 시 제목 앞 `✓` + amber 배경, 헤더에 `선택 삭제 (n)` 버튼 노출 확인
- [ ] `선택 삭제` → confirm → 삭제 성공 시 목록 갱신, `supervisor` 계정으로는 403 차단 확인
- [ ] `GET /api/cron/cleanup-ended` 호출 시 종료 후 30일 경과 설문만 삭제, 템플릿/데모는 제외 확인 (마이그레이션 후)

### B. 롤오버 가독성
- [ ] 설정 탭 `문항 불러오기` hover → amber (`bg-amber-600`) 확인
- [ ] 목록 탭 `편집 불러오기` / `응답 페이지` hover → `bg-zinc-900 text-white` 반전으로 글자 선명 확인 (다크모드에서는 `bg-white text-black` 반전)
- [ ] 상단 3탭(`① 설정 ② 편집 ③ 목록`) hover amber도 유지 확인

### C. 데모템플릿 구분
- [ ] 목록 탭 `데모템플릿` 필터 → 고정 6종 + `is_template=true`인 추가 템플릿만 노출, `전체/진행중/종료/초안`에서는 데모 제외 확인
- [ ] 데모 카드 보라 테두리/배지(`데모템플릿`/`추가 템플릿`), 체크박스 disabled(`데모템플릿은 삭제 불가`), `편집 불러오기` disabled 확인
- [ ] `JSON` 버튼이 목록에서 사라졌는지 확인

### D. 템플릿 승격 → 랜딩 아코디언 (총 15종)
- [ ] (마이그레이션 전) `GET /api/landing/templates`가 `{"templates":[],"warning":"is_template 마이그레이션 전 ..."}` 200을 반환하고 500 없이 동작하는지 확인 — 완료됨
- [ ] (마이그레이션 후) 목록에서 실설문(Form 연결, 초안 아님) 카드에 `★ 템플릿 등록` 버튼 노출 확인, 고정6종에는 미노출
- [ ] `★ 템플릿 등록` → 모달(카테고리/색상 선택) → 등록 → 목록에서 `추가 템플릿` 배지로 전환, `msg`에 `템플릿 등록됨` 표시 확인
- [ ] `http://localhost:3000/` 랜딩 `#templates` → 기본 6종 유지 확인, 아래에 `+ 추가 템플릿 보기 (n/9)` 버튼 노출(동적 n개 있을 때만) 확인
- [ ] `+` 버튼 클릭 → violet 카드 그리드 펼침, 다시 클릭 → 접힘, `n`이 9개일 때 10번째 등록 시 `400 최대 9개` 차단 메시지 확인
- [ ] 추가 템플릿 카드 클릭 → `/s/{id}` 응답 페이지 정상 진입 확인
- [ ] `★ 해제` → 랜딩 아코디언에서 해당 카드 제거 확인

### E. 기본 기능 회귀
- [ ] 랜딩 6종 카드 클릭 → 각 `/s/{id}` 데모 설문 렌더링 및 제출 플로우 정상 확인
- [ ] 분류 링크 3케이스(교수명+과목명 hidden, 학과명 select, 분류 없음) → `/s/{id}?key=값` 제출 후 시트 `taxonomy_*` 컬럼 확인 (08-25 이월)
- [ ] 문항 제어 3케이스(개인정보 `필수+허용값:동의함` 차단, 날짜/URL 검증, 조건부 섹션이동 `END`) 확인 (08-26 이월)

> 테스트 결과를 `devlog/2026-08-28.md` 아래에 댓글 또는 별도 파일로 남겨주시면 다음 세션에서 실패 항목을 우선 수정합니다.

---

## 2026-08-28 추가 — 대학 행사·이벤트 분기/GRID/팝업 종합 수정

### 1) 제목/주소 노출 (`57082f3`)
- `forms.ts:40` `documentTitle` fallback 추가, Supabase `983d...` title `대학 행사·이벤트 → 대학 행사·이벤트 설문` 업데이트, `SurveyRenderer`가 `surveys.title` 우선 — `설문 1Ifoy...` 주소 노출 제거

### 2) 분기 자동 추론 (`3cd9bde`)
- `SurveyRenderer` `inferBranchFromRaw` — `raw.items[].questionItem.question.choiceQuestion.options[].goToSectionId`를 `pageBreakIds` 순서로 페이지 인덱스에 매핑, `300d78a4(1번)` `예→1` `아니오→2` 자동 생성, DB 마이그레이션 전에도 동작

### 3) GRID/RANK 매트릭스 (`2498c2a` + `10a3479`)
- `forms.ts:57` `questionGroupItem.grid`를 `GRID {gridColumns 1순위~5순위, gridRows 10개}`로 파싱, `unsupported 0`, `constants`에 `GRID` 추가
- `questionConfig` GRID 행별 필수 검증, `SurveyRenderer` 매트릭스 테이블 렌더, `submit` 시 `rowId` 평탄화 — 어떤 설문의 순위형도 무수정 지원
- `10a3479` 10개 항목을 5개 순위로 평가하므로 **행별 독립, 중복 허용**으로 수정 — 이전 중복 차단(5개만 가능) 제거, `opacity-40`도 제거

### 4) 분기 건너뛴 문항 검증 제외 + 팝업 통일 (`eba762e`) — 실수 재발 방지 기록
- **버그**: `아니오` 선택 시 `2~13번` 건너뛰고 `14번` 이후 제출 시 `3번(총 몇 회 정도)` 필수 오류 — 원인은 `visited` 기반 검증이 건너뛴 페이지를 visited에 포함하거나, 빠른 클릭 시 overrides 미설정으로 분기 미적용
- **수정**: `submit`에서 `visited` 대신 **분기 도달 가능 페이지(reachable) 시뮬레이션**으로 검증 — `getNextPageIndex`를 `0→next` 반복해 도달 가능한 페이지만 `visitedIds`에 포함, 건너뛴 페이지는 `visited`에 있어도 제외. `page` 현재 페이지도 reachable에 포함. 이로써 `아니오` 시 `3번` 검증 스킵.
- **팝업 통일(전 설문)**: 상단 `status` 텍스트는 스크롤 시 안 보여 여러번 제출 유발 → **화면 세로 중앙 모달**(`globalPopup` `fixed inset-0 bg-black/50`)로 통일, 문항별 오류는 **해당 문항 바로 아래 빨간 인라인 팝업**(`fieldErrors[q.id]` `bg-red-50`)으로 표시, `scrollIntoView({block:"center", scroll-mt-24})`로 가리지 않게 노출. `setAns/setGridAns` 시 해당 문항 오류 자동 해제, `handleNext/Prev` 시 전체 팝업 클리어. `handleNext`/`submit` 모든 검증 경로가 `setFieldErrorFromMessage`/`showGlobal`을 통하도록 통일.
- **재발 방지**: 분기 로직은 `inferBranchFromRaw`로 구글 폼의 `goToSectionId`를 자동 추론하므로 향후 분기 추가 시 코드 수정 없이 반영. 검증은 `reachable` 기반으로만 수행하도록 주석으로 명시.

### 검증
- `npm run build` 14 routes 통과, `GET /api/forms/983d...` `questions 27, unsupported 0, gridRows 10, cols 5` 확인, `http://localhost:3000/s/983d...`에서 `예→2번~13번`, `아니오→14번` 분기 및 GRID 매트릭스 10행 제출 정상, 중앙 팝업 + 인라인 팝업 동작 확인.

### 5) 서버 분기 검증 추가 수정 (`bb08274`) — 14번 이후 제출 시 2번 오류 재발 방지
- **버그**: `아니오`로 14번 이후 제출 시 클라이언트는 reachable로 2번 검증 스킵했으나 서버는 DB `question_overrides`가 비어 있어 모든 문항 필수로 검증 → `400 필수: 2번` 오류가 정상 응답 뒤에 표시됨
- **수정**: `/api/submit`에서도 `forms.googleapis` raw의 `goToSectionId`를 `pageBreakIds`로 추론해 분기 생성, `parsed.sectionBreaks`로 `allPages` 구성 후 `getNextPageIndex` 시뮬레이션으로 `reachable` 계산, `visitedIds`는 `reachable`만 포함 — 2~13번 검증 제외. 테스트 `아니오+14번 이후 200`, `예+page1 누락 400` 확인.
- **제출 대기**: `9f3b74e` `isSubmitting`+중앙 모달 `제출 중 입니다. 잠시 기다려 주세요.` + 버튼 `disabled` + 스피너로 중복 클릭 방지, `try/finally`로 대기 중 재클릭 차단.

