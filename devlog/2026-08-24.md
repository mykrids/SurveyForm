# 2026-08-24 작업 기록

## 작업 목표
- 랜딩 페이지 krids 스타일 전환 및 문의/템플릿 정리
- 관리자 패널 가독성/캐시/테마/요금제 개선
- 관리자 도움말 대폭 강화 및 시스템 설정 자동 입력
- 관리자 인증(Administrator/Supervisor) + 이메일 오타 사전 차단 + 구글폼 분리 운영
- Supervisor 다중 계정 및 Administrator의 비밀번호 관리 기능

## 진행 상황
- `opencode-auto-resume` 정상 동작 확인 후 연속 Vibe Coding으로 진행, 각 단계마다 `npm run build` 검증 및 `git push` 수행
- `next dev`의 Turbopack 메모리 캐시로 인한 반영 지연 2회 발생 → `kill` + `rm -rf .next` + 재시작으로 해결 (`curl`로 정적 HTML 내 문구 직접 검증)

## 완료된 것

### 1) 랜딩 페이지 수정 (`8a65600`)
- `src/components/Landing.tsx:19` 히어로 우측 `응답 미리보기 (커스텀 UI)` 박스 전체 삭제, 그리드→단일 컬럼(`max-w-3xl`)으로 변경
- `src/lib/constants.ts:30` 배지 `모아폼 스타일 → krids 스타일`, `32` `あなたの → 귀교의`
- `44-51` 템플릿 교체: `신입 채용 지원서 → 강의 평가`, `제품 피드백 → 대학생활과 수업 만족도 조사`, `시장 조사 → 교육 신청서`, 카드 문구 `xx문항 · 모아폼 스타일 → krids 스타일`
- `75-90` 문의 섹션(`#contact`) 신설 — KRIDS 4개 불릿 + 메일 아이콘 + `krids.org@gmail.com` (`mailto:`)
- `91-99` 푸터: 메일 아이콘 + `krids 스타일 커스텀 설문 SaaS`로 교체, 헤더 네비에 `문의` 앵커 추가

### 2) 관리자 패널 가독성 (`9114c18`, `99c2d51`)
- `src/app/globals.css:15` 다크모드 강제 라이트 고정 및 `input/select/textarea`에 `bg-white !important`·`#171717` 강제 → 흰 배경에서 글자 안 보임 해결
- `src/components/AdminPanel.tsx:52` 컨테이너·라벨·인풋에 `text-zinc-900`·`border-zinc-300`·`placeholder:text-zinc-400` 명시
- 이후 `99c2d51`에서 `html { color-scheme: light }` 및 `!important` 추가로 브라우저 다크 스타일 완전 차단

### 3) 전역 테마 및 N 버튼 한글화 (`985aed8`)
- `next.config.ts:3` `devIndicators: false`로 좌하단 영문 `N` 버튼 숨김
- `src/components/ThemeProvider.tsx:1` 신설 — `localStorage:krids-theme` 기반 `documentElement.classList.toggle('dark')`, 좌하단 한글 토글 `다크 모드/라이트 모드` + `테마는 페이지 전체에 적용됩니다` 문구로 "기능 창만 바뀜" 해소
- `src/app/globals.css:1` `@custom-variant dark`, `:root/.dark` 변수, `.dark input` 등 전역 다크 스타일 추가
- `src/app/layout.tsx:20` `lang="ko"`, `ThemeProvider` 래핑, `Landing.tsx`/`AdminPanel.tsx`/`SurveyRenderer.tsx`에 `dark:` 클래스 전방위 추가
- `AdminPanel.tsx:107`에 `도움말 · 관리자 이용 안내` 카드 추가

### 4) 요금제 문구 수정 (`c44db04`)
- `src/lib/constants.ts:54` Pro `설문 무제한 → 고객 요청 맞춤 설문`
- `55` Enterprise `SSO / 팀 관리 → 원하는 서비스 협의`, `SLA 보장 → 보고서 형식 고객 맞춤`, `영업 문의 → pro 확장 문의`

### 5) 관리자 패널 대규모 개선 (`379423e`)
- `src/components/AdminPanel.tsx:20` `CURRENT_VALUES` 카드로 Supabase(`bpvvxsrtfigphxzoztjm`), 서비스계정(`survey-form@velvety...`), GAS(`AKfycbz…`), Resend(`noreply@krids.org`), GitHub/Vercel 현황을 마스킹 표시
- 폼 라벨에 `시트 생성 시 갱신` 배지 및 예시(`1FAIpQLSd…`, `https://docs.google.com/forms/d/...`) 추가, `시작/종료 일시`·`GAS URL` 등은 갱신 대상, `관리자 이메일` 등은 항상 동일 구분
- `종료 메시지 프리셋` 선택 시 미리보기 박스 및 `1~10번 전체 보기` details, `중복 방지`는 `none/cookie/email/email_verified`별 방식·응답자 할 일(`그냥 응답하면 됩니다`)을 파란 박스와 details로 상세 설명
- `1️⃣ 구글폼 먼저 만들어야 하나요?` 5단계, `2️⃣ 템플릿 만드는 방법` 복제 방식, `3️⃣ 1~10번` 전체 리스트, `4️⃣ 중복 방지` 4종 상세, 빠른 체크리스트까지 추가
- GitHub push protection으로 Resend 키 원문 노출 차단 → 마스킹(`re_***` 등)으로 재커밋

### 6) 관리자 인증 + 이메일 오타 차단 + 분리 운영 (`eba0811`)
- `src/lib/auth.ts:1` `ADMIN_ID/PASSWORD`, `SUPERVISOR_ID/PASSWORD` (기본 `administrator/krids2026!`, `supervisor/krids2026@supervisor`) 및 `HMAC-SHA256` 서명 쿠키(`krids_admin_token`, 7일)
- `src/app/api/admin/login|logout|me` 3종 및 `src/components/AdminGate.tsx:1`로 `/admin` 접근 시 ID/PW 요구, 역할 배지·로그아웃, `src/app/api/admin/surveys`를 인증 필수로 보호
- `src/lib/emailTypo.ts:1` `@` 누락 + `never.com→naver.com` 등 30여개 매핑 + 레벤슈테인 거리 1~2 + 점 누락 검사, `SurveyRenderer.tsx:23`와 `AdminPanel.tsx:64`에서 빨간 테두리·`‘naver.com’(으)로 교정` 버튼, `src/app/api/submit/route.ts:21`에서 서버 400 차단 (`curl test@never.com → naver.com` 검증)
- `AdminGate.tsx:76`·`AdminPanel.tsx:95`에 `구글폼 편집은 drive.google.com에서 직접, 관리자 대시보드는 Form ID 연결만` 분리 운영 배너 명시

### 7) Supervisor 다중 계정 및 Administrator 관리 (`7af0885`, `6c75941`)
- `supabase/schema.sql:27` `admin_users` 테이블 추가
- `src/lib/adminUsers.ts:1` Supabase 우선, 없으면 `.data/admin-users.json` + `globalThis.__adminUsers` fallback으로 `getSupervisors`/`verifySupervisor`/`create`/`update`/`delete` 구현 (`SUPERVISOR_LIMIT` 기본 5)
- `src/app/api/admin/users/route.ts:1` `GET/POST/PUT/DELETE` (Administrator 전용), `src/components/AdminUserManager.tsx:1`에서 목록·생성·비밀번호 변경/리셋·삭제 UI 제공, `src/components/AdminGate.tsx:32`에서 Administrator 로그인 시에만 노출
- 테이블 미생성 시 `Could not find the table` 에러를 파일 fallback으로 처리 (`6c75941`), `curl`로 `supervisor2` 생성→리셋→로그인→삭제 전 과정 검증
- `.env.example:11`에 `ADMIN_ID/PASSWORD`, `SUPERVISOR_ID/PASSWORD`, `SUPERVISOR_LIMIT`, `AUTH_SECRET` 예시 추가 (`.gitignore`로 `.env*` 무시되나 `git add -f`로 강제 커밋)

### 8) 불필요 내용 삭제 (`f35fd94`)
- `src/components/AdminPanel.tsx:181` `※ 전체 1~10번 목록은 아래 ‘종료 메시지 1~10번 상세’에서 확인` 문구 삭제
- `259-270` `3️⃣ 설문 기간 아래 1~10번은? — 종료 메시지 프리셋 전체 보기` details 섹션 전체 삭제, 기존 `4️⃣ 중복 방지`를 `3️⃣`로 재번호 — `curl`로 미노출 확인

### 9) 템플릿 카드 연결 방식 질의 응답
- 랜딩 6개 카드는 `src/lib/constants.ts:44` 고정 예시(`href="/s/demo"` `src/components/Landing.tsx:50`), 실제 설문은 `/admin`에서 `Form ID` 등록 후 생성되는 `/s/<설문ID>` 링크로 응답 — 두 개념 분리 설명, `KRIDS_템플릿_6종` 폴더 생성 → 폼 6개 생성 → 서비스계정 뷰어 공유 → Form ID를 관리자 패널에 붙여넣어 `krids 링크` 배포하는 6단계 순서로 문서화 (코드 변경 없음, 필요 시 `templates`를 Supabase 동적 로딩으로 교체 가능)

### 10) demo 템플릿 시트 분리 (`b432361`)
- `src/app/api/submit/route.ts:76` `isDemo = surveyId === "demo"` 분기 추가로 demo 제출 시 `GAS 쓰기 / 확인 메일 / 중복 로그` 모두 스킵 — `mockForm` 4문항은 UI 미리보기 전용, 구글시트 저장과 무관함을 보장 (`console.log("[submit] demo 모드 — ... 스킵")`)

## 빌드/배포
- 매 단계 `npm run build` 통과 (Turbopack, TypeScript, 10개 라우트: `/`, `/admin`, `/api/admin/login|logout|me|surveys|users`, `/api/cron/generate-reports`, `/api/forms/[id]`, `/api/submit`, `/s/[id]`)
- `git push` 11회 수행: `8a65600` → `9114c18` → `99c2d51` → `985aed8` → `c44db04` → `379423e` → `eba0811` → `7af0885` → `6c75941` → `f35fd94` → `b432361` (현재 HEAD `b432361`)

## 미완료 / 다음 할 일 (내일 진행)
- Supabase `admin_users` 테이블을 프로덕션 DB에 적용 (SQL Editor에서 `supabase/schema.sql` 실행) — 미적용 시 Vercel에서는 파일시스템 휘발로 Supervisor 추가가 재시작 시 유실될 수 있음
- Resend `email_verified` 실제 인증 링크 발송 플로우 (현재는 `email`과 동일 동작, `src/lib/emailTypo.ts:72`에 `notYet` 표기)
- 랜딩 템플릿 카드와 실제 `surveys` ID 동적 매핑 (현재 `/s/demo` 고정) — 필요 시 구현
- Vercel 환경변수에 `ADMIN_ID/PASSWORD`, `SUPERVISOR_ID/PASSWORD`, `SUPERVISOR_LIMIT`, `AUTH_SECRET` 등록 및 재배포 확인

## 참고
- `.env` 실제 값: Supabase `bpvvxsrtfigphxzoztjm`, 서비스계정 `survey-form@velvety-maker-506214-u5`, GAS `AKfycbz.../exec`, Resend `noreply@krids.org`, Cron `344b6d...` — 코드에서는 마스킹 처리
- dev 서버 캐시 이슈 3회 발생 시 `rm -rf .next`로 해결, `curl`로 HTML 내 문구 직접 검증하는 패턴 유지
- 2026-08-24 22시 기준 dev 서버 `nohup npm run dev` 유지 중, 내일 `devlog` 및 `git log` 확인 후 재개
