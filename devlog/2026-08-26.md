# 2026-08-26 작업 기록

## 세션 시작 점검
- `devlog/2026-08-25.md:80` 미완료 4건 확인, `git log` HEAD `5d222e4` (야간 7건 추가: 설명문 볼드·7점척도 가로·폭 816px·페이지네이션·체크박스 3개), `git status` clean, origin/main 동기화
- `npm run build` 12 라우트 정상, dev 서버 중단(000) 확인

## 작업 목표 (사용자 지시)
- 구글폼 제어 로직(필수·조건부 섹션이동·유효성 검사) 타당성 검증 — 실행 코드 추출 불가 여부, 대시보드 설문 편집 프리셋 방식 vs 구글폼 모방 중 합리적안 제안
- 검증 결과 기반 설문 편집 기능 구현 (요청: 쉽고 수정 불필요한 안전한 방식, OFF 기본, 필요 시 ON)
- 브라우저 화면 검증 및 오늘 할일 이어가기

## 진행 — 기획 검증 (plan 모드, 읽기 전용)

### 구글 제어 로직 추출 검증
- `src/lib/forms.ts:36` `parseGoogleFormResponse`는 `required/paragraph/choice/options/scale`만 파싱, 검증식·분기 실행 코드는 API 미노출 — `src/app/api/forms/[id]/route.ts:51` `forms.googleapis.com/v1/forms/{id}`는 선언적 메타만 제공, `gstatic` 클라이언트 JS는 난독화로 추출 불가 확인
- 일부 선언값(`textValidation`, `goToSection`)은 API에 존재하나 현 파서 미사용 + 버전별 불완전 — 완전 모방 시 유지보수 비용 높음

### 제안 비교
- **A. 프리셋 체크 방식(권장)**: 구조는 Google import, 제어는 `question_overrides jsonb`가 Single Source of Truth — OFF면 현 동작과 100% 동일, ON 시에만 검증/분기 동작. 수정 없이 안정 운영 가능
- **B. 구글폼 그대로 모방**: API 불완전으로 1:1 재현 불가, 템플릿 6종마다 구글에서 검증 설정 필요 — 일원화 이점 상실

### 확정안 (사용자 승인)
- 프리셋 9종 전부 OFF 기본: `email/phone/numeric/length/range/date/url/regex/allowedValues` — 개인정보 동의는 `필수+allowedValues:동의함`으로 해당 문항에 머무르게 차단(종료 이동 없음)
- 조건부 이동 `branchEnabled OFF` 기본, ON 시 `선택지→섹션 인덱스/END` 맵으로 분기
- Google `required`는 초기값으로만 import, 이후 대시보드 토글이 절대값

## 완료 — 설문 편집 구현 (`cd9d1e5`)

### 1) DB (`supabase/schema.sql:15`)
- `question_overrides jsonb not null default '{}'::jsonb` 추가 — `taxonomy_fields` 패턴과 동일, 별도 테이블 없이 마이그레이션 `alter table surveys add column if not exists question_overrides jsonb ...`

### 2) `src/lib/questionConfig.ts:1` 신설
- `ValidationPreset` 9종, `PRESET_LABELS`, `defaultValidations()`(전체 OFF), `ensureDefaults`, `validateSingleValue`(이메일 regex·전화 010·숫자·길이·범위·날짜 YYYY-MM-DD·URL·regex·허용값), `validateAnswers`(visitedIds 지원), `validateOverrides`(50문항/20룰 제한), `getNextPageIndex`(분기 `END` 포함)

### 3) API
- `src/app/api/admin/surveys/route.ts:4,34,60,77` — `question_overrides` 저장 시 `validateOverrides`, fallback(미컬럼 시 taxonomy/overrides 없이 재시도)
- `src/app/api/surveys/[id]/route.ts:20` — 공개 조회에 `question_overrides` 포함
- `src/app/api/submit/route.ts:9,66` — 서버 이중 검증: `validateOverrides` + Forms API fetch로 `parsedQuestions` 확보 후 `validateAnswers`, 실패 시 스킵(안전)
- `src/app/api/forms/[id]/route.ts:65` — 주석 보강(merge는 클라이언트 수행)

### 4) 관리자 패널 (`src/components/AdminPanel.tsx:6,19,41,78,221`)
- `📝 설문 편집` 섹션: `문항 불러오기` 버튼 → `/api/forms/{formId}`로 `ParsedQuestion[]` 로드, 섹션 수 계산, 구글 `required` 초기값 반영
- 문항별: 필수 토글(Google 필수 배지·필수 적용 배지), 검증 프리셋 9종 체크+파라미터(min/max·pattern·허용값), 조건부 이동 ON/OFF + 선택지→섹션/`END` select — 전부 OFF 기본
- 저장 시 `validateOverrides` 통과 후 `question_overrides` 포함 POST

### 5) 응답 페이지 (`src/components/SurveyRenderer.tsx:6,17,36,60,161,194`)
- `overrides` state + `visited` Set, `/api/surveys/{id}`에서 `question_overrides` 로드
- `handleNext`: 현재 페이지 문항만 `validateAnswers`로 차지단(허용값 미동의 시 떠나지 못함) → `getNextPageIndex`로 분기(END 시 `branchEnded` 상태), `visited` 갱신
- `submit`: `branchEnded` 시 차단, `visited` 기반 전체 문항 재검증(분기 스킵 문항 제외), `effReq`(overrides 우선)로 별표·required 반영
- SCALE/라디오/체크박스 required도 `effReq`로 통일

## 빌드/배포/검증
- `npm run build` 2회 통과 (6.0s/16.4s, 12 라우트: `/`, `/admin`, `/api/admin/*`, `/api/cron/*`, `/api/forms/[id]`, `/api/submit`, `/api/surveys/[id]`, `/go/[id]`, `/s/[id]`) — Turbopack
- `git push` 1회: `5d222e4..cd9d1e5` (HEAD `cd9d1e5`)
- dev 서버 재기동: `rm -rf .next && nohup npm run dev` → `Ready in 5.5s`, `GET / 200`, `GET /s/demo 200`, `GET /admin 200` 확인
- 브라우저 수동 확인 절차 안내: `/admin` 로그인 → Form ID 입력 → 문항 불러오기 → 필수+허용값/날짜/URL ON 테스트 → `/s/{id}`에서 다음 차단·분기 확인

## 미완료 / 다음 할 일
- Supabase 프로덕션 마이그레이션: `taxonomy_fields` + `question_overrides` + `admin_users` (SQL Editor)
- Vercel env 6종(`ADMIN_ID/PASSWORD`, `SUPERVISOR_*`, `AUTH_SECRET`) 점검 및 재배포 후 6템플릿 실클릭 최종 확인
- 분류 링크 3케이스 + 문항 제어 3케이스(개인정보 동의 차단·날짜/URL 검증·조건부 섹션이동) 실전 테스트
- `email_verified` 인증 링크 플로우 (현재 `email`과 동일)

## 참고
- 모든 검증/분기 OFF면 기존 동작과 동일 — 템플릿 6종 호환, 향후 프리셋 추가 시 스키마 변경 없음(상수 한 줄 추가)
- `.env` `velvety-maker-506214-u5` 유지, GAS 404는 WebApp 미배포 상태와 무관

---

## 2026-08-26 추가 — 관리자 메뉴 분리 및 목록 스마트 관리 (`f64406d`)

### 요청
- 기존 단일 페이지 가독성 저하 → '관리자 설정 패널'과 '설문 편집'을 별도 메뉴로 분리, 탭 클릭 시 분리 관리
- 6개 템플릿 외 실설문 등록 시 목록 길어짐 → 똑똑한 관리(검색/필터/페이지) 필요
- 실제 설문 등록→편집 순서 상세 나열 요청

### 구현
- `src/components/AdminPanel.tsx:41` 3탭 구조 `settings | edit | list` + 상태 `search/statusFilter/listPage`
- 탭 ① 설정: 제목·Form ID·기간·GAS URL·분류 등 기존 설정 폼 유지, `다음: 설문 편집 →` 버튼으로 흐름 연결
- 탭 ② 편집: Form ID 기반 `문항 불러오기` + 문항별 필수/검증 9종/조건부 이동 — 기존 편집 UI를 독립 탭으로 이동
- 탭 ③ 목록: `filtered` useMemo(검색+상태 필터) + `PAGE_SIZE=6` 페이지네이션 + 상태 배지(초안/종료) + `편집 불러오기`로 설정/편집 탭에 재로드, 새로고침 지원
- 시스템 설정은 `details` 접기 가능으로 상단 공간 절약
- 도움말 1️⃣을 9단계 실전 순서(구글 생성→뷰어 공유→Form ID 복사→시트+GAS 배포→설정 탭 입력→편집 탭 문항 편집→저장→배포 링크→수정 시 편집 불러오기)로 전면 개편

### 검증
- `npm run build` 통과 (12 라우트), `git push` `f64406d`

